This service is used to import datasets into the geoserver

# test 

Integration test can be run via docker-compose:

```shell
# first install dependencies
pip install -r requirements
#then run the test 
python test_import.py
```
